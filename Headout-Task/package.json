{
  "name": "headout-task",
  "version": "1.0.0",
  "description": "A simple HTTP server implemented in Express.js that fulfills the specified requirements. The server responds to incoming GET requests on the endpoint `/data` and accepts two query parameters: `n` for file name and `m` for line number.",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "express": "^4.18.2"
  }
}
